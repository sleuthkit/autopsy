Move the ingest/ and report/ to the directory opened by Tools > Python Plugins
The plugins must be inside a directory inside the one opened by Tools > Python Plugins 
NOTE: The directory opened by Tools > Plugins is cleared every time the project is cleaned.